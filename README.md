# Jai Hanuman Ji Property

A static single-page React website for Jai Hanuman Ji Property.

## Run locally

```bash
npm install
npm run dev
```

The static React site runs at `http://localhost:5173`.

## Build

```bash
npm run build
```

The deployable static files are created in `dist`.
